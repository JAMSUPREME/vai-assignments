<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Your cool web site</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav" id="nav-bar">
                <li class="@(ViewBag.PageName == "Index" ? "active" : "")">@Html.ActionLink("Home", "Index")</li>
                <li class="@(ViewBag.PageName == "MVC" ? "active" : "")">@Html.ActionLink("MVC Basics", "MVC")</li>
                <li class="@(ViewBag.PageName == "JQuery" ? "active" : "")">@Html.ActionLink("JQuery Basics", "JQuery")</li>
                <li class="@(ViewBag.PageName == "Bootstrap" ? "active" : "")">@Html.ActionLink("Bootstrap Basics", "Bootstrap")</li>
            </ul>
        </div>
    </div>
</nav>